{% extends 'base.html.twig' %}

{% block title %}Dungeon{% endblock %}

{% block body %}
    <h1>Dungeon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dungeon.id }}</td>
            </tr>
            <tr>
                <th>Name_dungeon</th>
                <td>{{ dungeon.nameDungeon }}</td>
            </tr>
            <tr>
                <th>Descr_dungeon</th>
                <td>{{ dungeon.descrDungeon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dungeon_index') }}">back to list</a>

    <a href="{{ path('dungeon_edit', {'id': dungeon.id}) }}">edit</a>

    {{ include('dungeon/_delete_form.html.twig') }}
{% endblock %}
